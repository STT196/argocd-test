apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sumana.fullname" . }}-test-connection"
  labels:
    {{- include "sumana.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ .Values.appName }}-backend:{{ .Values.backend.port }}']
  restartPolicy: Never
